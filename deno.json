{
  "tasks": {
    "build_wasm": "cd tokenizers && deno run -A --unstable scripts/build.ts tokenizers && cd ..",
    "example_tokenizer": "deno run -A --unstable examples/tokenizer.ts",
    "example_gpt_neo": "deno run -A --unstable examples/gpt-neo.ts",
    "example_gpt2": "deno run -A --unstable examples/gpt2.ts",
    "example_chatbot": "deno run -A --unstable examples/chatbot.ts",
    "example_distilbert": "deno run -A --unstable examples/distilbert.ts",
    "example_t5": "deno run -A --unstable examples/t5.ts",
    "example_whisper": "deno run -A --unstable examples/whisper.ts"
  }
}
